This directory is reserved for the upstream `grokzhuce` source snapshot.

The production Grok integration in `src/core/grok/` does not import files from
this directory at runtime. When the upstream source is available, place it here
for reference during future adapter updates.
